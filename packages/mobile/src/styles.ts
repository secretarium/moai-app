import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#eb7473'
    },
    backgroundImage: {
        height: '100%',
        width: '100%',
        resizeMode: 'contain'
    }
});
