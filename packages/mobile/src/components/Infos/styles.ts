import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    card: {
        borderRadius: 4,
        overflow: 'hidden',
        alignItems: 'center',
        maxWidth: '90%',
        marginHorizontal: 12,
        marginVertical: 6,
        padding: 20
    }
});